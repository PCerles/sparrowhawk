import '../byte.grm' as b;
import '../util.grm' as u;

d = b.kDigit;
q = u.q;

# 300 -> cardinal { integer: "300"}

fraction =
  u.I["fraction { "]
  u.I["numerator: " q]
  d+
  u.I[q]
  u.D["/"]
  u.I["denominator: " q]
  d+
  u.I[q]
  u.I[" }"]
;

export FRACTION = Optimize[fraction];
